// Navbar component for the portfolio.
// Requirements for Copilot:
// - Sticky top navbar with a translucent dark background and backdrop blur.
// - Left: logo text (e.g., "ES" or "Ekonkar Singh") that scrolls to the top.
// - Right: horizontal nav links: About, Projects, Experience, Contact (scroll to section ids).
// - On mobile, collapse into a simple menu (hamburger that toggles a dropdown).
// - Use Tailwind classes for styling.

"use client";

export default function Navbar() {
  return (
    <header className="sticky top-0 z-40">
      {/* Copilot: implement the navbar layout with responsive behavior and smooth scrolling links. */}
    </header>
  );
}
